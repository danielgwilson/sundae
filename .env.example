# Copy to .env.local for local dev. Never commit real secrets.

NEXT_PUBLIC_APP_URL="http://localhost:3000"
# Optional server-side base URL for generating absolute links (emails, etc.).
# In production, set to "https://sundae.to".
APP_URL=

# Auth (NextAuth/Auth.js)
# Generate with: openssl rand -base64 32
AUTH_SECRET=

# Google OAuth (preferred names)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Compatibility env var names (some codebases/tools still use these)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# NEXTAUTH_SECRET=

# Postgres / Drizzle
DATABASE_URL=

# Often used for serverless poolers (Supabase, Vercel Postgres, etc.)
POSTGRES_URL_NON_POOLING=
POSTGRES_URL=

# Optional: used to hash IPs for analytics (falls back to AUTH_SECRET if unset)
ANALYTICS_SALT=

# Email (Resend)
RESEND_API_KEY=
# Example: "Sundae <hello@sundae.to>"
RESEND_FROM=
