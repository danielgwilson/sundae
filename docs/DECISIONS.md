# Decisions (sundae)

- Use a `workspaces` + `workspace_members` model even in v1 (single-user) to keep the path open for future teams/roles.
- Model page content as typed `blocks` with a `data` JSON column for fast iteration and extensibility.
